---
import PostMeta from '~/components/PostMeta.astro'
import type { Post } from '~/types'

interface Props {
  post: Post
}

const { post } = Astro.props
---

<article class="prose transition-colors duration-200 ease-in-out">
  <PostMeta post={post} />
  <slot />
</article>
